@model IEnumerable<VoterAnalysis.Models.Staff>

@{
    ViewData["Title"] = "View";
}

<h1>View</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="AssignStaffPrecinct" asp-route-id="@item.Id">Assign Precinct</a>
            </td>
        </tr>
}
    </tbody>
</table>
